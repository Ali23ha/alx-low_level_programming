#include "main.h"

/**
 * _isdigit - checks for a digit 
 * @c: parameter to be checked (0 through 9)
 * author: ali_hasan
 * Return: 1 if true, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
