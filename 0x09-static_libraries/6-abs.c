#include "main.h"

/**
 * _abs - find the absolute value of an integer
 * @n: parameter int
 * author: ali_hasan
 * Return: the absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
